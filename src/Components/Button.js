import React from "react";

const Button = ({ button, icon, type }) => {
  return (
    <div>
      <a class="btn" href="/">
        I am a button
      </a>
    </div>
  );
};

export default Button;
